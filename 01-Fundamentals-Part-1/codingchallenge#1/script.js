// OPERATORS CHALLENGE
// DATA 1

let markWeight = 78;
let markHeight = 1.69;
let johnWeight = 92;
let johnHeight = 1.95;
let markBmi = markWeight / (markHeight ** 2); 
let johnBmi = johnWeight / (johnHeight ** 2); 

console.log(markBmi,johnBmi);
markHigherBMI = console.log(markBmi>johnBmi);

// DATA 2
markWeight = 95;
markHeight = 1.88;
johnWeight = 85;
johnHeight = 1.76;

markBmi = markWeight / (markHeight ** 2); 
johnBmi = johnWeight / (johnHeight ** 2); 

console.log(markBmi,johnBmi);
markHigherBMI = console.log(markBmi>johnBmi);

/* Improvements
- use const always
- comment out the first data and add second data right after while using const */
